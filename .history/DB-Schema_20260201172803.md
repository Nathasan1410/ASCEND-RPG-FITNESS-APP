### 2. `DB_SCHEMA_DRAFT.md` (The Backbone)
Supabase itu Relational DB. Kalau AI salah bikin relasi (Foreign Key), fitur Leaderboard kamu gagal. Berikan draft ini ke Deep Research/OpenCode sebagai panduan.

**Isi File:**
```markdown
# 🗄️ Database Schema Draft (Supabase)

## Tables

1. **profiles** (Extends Auth)
   - id (uuid, PK, ref auth.users)
   - username (text, unique)
   - class (enum: 'Novice', 'Striker', 'Tank', 'Assassin')
   - level (int, default 1)
   - current_xp (int, default 0)
   - rank_title (text, e.g., 'E-Rank Hunter')
   - stats_strength (int)
   - stats_agility (int)
   - stats_stamina (int)

2. **quests** (Generated Workouts)
   - id (uuid, PK)
   - user_id (uuid, FK)
   - plan_json (jsonb) -- Stores the full Groq output
   - status (enum: 'Active', 'Completed', 'Failed')
   - created_at (timestamp)

3. **logs** (Workout History)
   - id (uuid, PK)
   - quest_id (uuid, FK)
   - user_feedback (text) -- "Sakit banget bang"
   - duration_actual (int)
   - xp_awarded (int) -- Calculated by Opik Judge
   - opik_trace_id (text) -- Link to Opik dashboard

## Views
1. **leaderboard_season_1**
   - Selects username, level, xp from profiles
   - Ordered by Level DESC, XP DESC